{##
 # First check address
 #
 # @ListChild (list="checkout.shipping.selected.sub.shipping", weight="110")
 #}
{% if this.isTaxCloudAddressVerificationEnabled() %}
  <div class="taxcloud-check-box">
    {% if this.isShippingEnabled() %}
      {{ widget('XLite\\View\\Button\\Regular', label=t('Check address'), style='taxcloud-check shipping') }} 
    {% else %}
      {{ widget('XLite\\View\\Button\\Regular', label=t('Check address'), style='taxcloud-check billing') }}
    {% endif %}
  </div>
{% endif %}

